# PRODUCTION ENVIRONMENT TEMPLATE
# Questo file viene usato da deploy-first-time.sh

APP_NAME="Scuola di Danza"
APP_ENV=production
APP_KEY=                          # Generato automaticamente
APP_DEBUG=false
APP_TIMEZONE=Europe/Rome
APP_URL=https://YOUR_DOMAIN       # Configurato da script
APP_LOCALE=it

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=scuoladidanza
DB_USERNAME=scuoladidanza
DB_PASSWORD=                      # Configurato da script

CACHE_STORE=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# ARUBA SMTP
MAIL_MAILER=smtp
MAIL_HOST=smtps.aruba.it
MAIL_PORT=465
MAIL_USERNAME=                    # Configurato da script
MAIL_PASSWORD=                    # Configurato da script
MAIL_ENCRYPTION=ssl
MAIL_FROM_ADDRESS=                # Configurato da script
MAIL_FROM_NAME="${APP_NAME}"

LOG_CHANNEL=stack
LOG_LEVEL=error

SANCTUM_STATEFUL_DOMAINS=         # Configurato da script
SESSION_DOMAIN=                   # Configurato da script

# PayPal (configurare dopo)
PAYPAL_MODE=sandbox
PAYPAL_SANDBOX_CLIENT_ID=
PAYPAL_SANDBOX_SECRET=
PAYPAL_LIVE_CLIENT_ID=
PAYPAL_LIVE_SECRET=
