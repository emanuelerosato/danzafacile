<x-app-layout>

<div class="container mx-auto px-4 py-6">
    <div class="flex items-center justify-between mb-6">
        <div>
            <h2 class="font-semibold text-xl text-gray-800 leading-tight">
                Dettagli Orario - {{ $course->name }}
            </h2>
            <p class="text-sm text-gray-600 mt-1">
                Informazioni complete sul corso e programma
            </p>
        </div>
        <div class="flex items-center space-x-3">
            <a href="{{ route('admin.schedules.index') }}"
               class="inline-flex items-center px-4 py-2 bg-gray-600 text-white text-sm font-medium rounded-lg hover:bg-gray-700 transition-all duration-200">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                </svg>
                Torna al Calendario
            </a>
            <a href="{{ route('admin.schedules.manage') }}"
               class="inline-flex items-center px-4 py-2 bg-rose-600 text-white text-sm font-medium rounded-lg hover:bg-rose-700 transition-all duration-200">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
                </svg>
                Modifica Orario
            </a>
        </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Course Info -->
        <div class="lg:col-span-2 space-y-6">
            <!-- Basic Info Card -->
            <div class="bg-white rounded-lg shadow overflow-hidden">
                <div class="px-6 py-4 border-b border-gray-200 bg-gradient-to-r from-rose-50 to-purple-50">
                    <h3 class="text-lg font-semibold text-gray-900">Informazioni Corso</h3>
                </div>
                <div class="p-6">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Nome Corso</dt>
                            <dd class="mt-1 text-sm text-gray-900">{{ $course->name }}</dd>
                        </div>
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Livello</dt>
                            <dd class="mt-1">
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                    {{ $course->level }}
                                </span>
                            </dd>
                        </div>
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Istruttore</dt>
                            <dd class="mt-1 text-sm text-gray-900">{{ $course->instructor?->name ?? 'Non assegnato' }}</dd>
                        </div>
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Sala</dt>
                            <dd class="mt-1 text-sm text-gray-900">{{ $course->location ?: 'Non assegnata' }}</dd>
                        </div>
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Prezzo</dt>
                            <dd class="mt-1 text-sm text-gray-900">€{{ number_format($course->price, 2) }}</dd>
                        </div>
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Studenti</dt>
                            <dd class="mt-1 text-sm text-gray-900">{{ $course->enrollments->count() }}/{{ $course->max_students }}</dd>
                        </div>
                    </div>

                    @if($course->description)
                    <div class="mt-6">
                        <dt class="text-sm font-medium text-gray-500">Descrizione</dt>
                        <dd class="mt-1 text-sm text-gray-900">{{ $course->description }}</dd>
                    </div>
                    @endif
                </div>
            </div>

            <!-- Schedule Details -->
            <div class="bg-white rounded-lg shadow overflow-hidden">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h3 class="text-lg font-semibold text-gray-900">Dettagli Orario</h3>
                </div>
                <div class="p-6">
                    @if($course->schedule && count($scheduleData) > 0)
                    <div class="space-y-4">
                        <div>
                            <dt class="text-sm font-medium text-gray-500 mb-2">Orario Completo</dt>
                            <dd class="text-lg font-medium text-gray-900">{{ $course->schedule }}</dd>
                        </div>

                        <div>
                            <dt class="text-sm font-medium text-gray-500 mb-3">Programma Settimanale</dt>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                @foreach($scheduleData as $session)
                                <div class="bg-gradient-to-r from-rose-50 to-purple-50 border border-rose-200 rounded-lg p-4">
                                    <div class="flex items-center justify-between mb-2">
                                        <h4 class="font-medium text-gray-900">{{ $session['day'] }}</h4>
                                        <span class="text-sm bg-white px-2 py-1 rounded-full text-gray-600">
                                            {{ $session['time'] }}
                                        </span>
                                    </div>
                                    <div class="text-sm text-gray-600">
                                        <div class="flex items-center mb-1">
                                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                                            </svg>
                                            {{ $course->location ?: 'Sala da definire' }}
                                        </div>
                                        <div class="flex items-center">
                                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                                            </svg>
                                            {{ $course->instructor?->name ?? 'Istruttore da assegnare' }}
                                        </div>
                                    </div>
                                </div>
                                @endforeach
                            </div>
                        </div>
                    </div>
                    @else
                    <div class="text-center py-8 text-gray-400">
                        <svg class="w-12 h-12 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                        </svg>
                        <h3 class="text-lg font-medium mb-2">Nessun orario impostato</h3>
                        <p class="text-gray-500 mb-4">Il corso non ha ancora un orario definito</p>
                        <a href="{{ route('admin.schedules.manage') }}"
                           class="inline-flex items-center px-4 py-2 bg-rose-600 text-white text-sm font-medium rounded-lg hover:bg-rose-700">
                            Imposta Orario
                        </a>
                    </div>
                    @endif
                </div>
            </div>
        </div>

        <!-- Sidebar -->
        <div class="space-y-6">
            <!-- Quick Actions -->
            <div class="bg-white rounded-lg shadow p-6">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">Azioni Rapide</h3>
                <div class="space-y-3">
                    <a href="{{ route('admin.courses.edit', $course->id) }}"
                       class="w-full flex items-center justify-center px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 transition-all duration-200">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
                        </svg>
                        Modifica Corso
                    </a>
                    <a href="{{ route('admin.enrollments.index', ['course' => $course->id]) }}"
                       class="w-full flex items-center justify-center px-4 py-2 bg-green-600 text-white text-sm font-medium rounded-lg hover:bg-green-700 transition-all duration-200">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
                        </svg>
                        Gestisci Iscrizioni
                    </a>
                    <a href="{{ route('admin.attendance.course', $course->id) }}"
                       class="w-full flex items-center justify-center px-4 py-2 bg-purple-600 text-white text-sm font-medium rounded-lg hover:bg-purple-700 transition-all duration-200">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
                        </svg>
                        Presenze
                    </a>
                </div>
            </div>

            <!-- Course Statistics -->
            <div class="bg-white rounded-lg shadow p-6">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">Statistiche Corso</h3>
                <div class="space-y-4">
                    <div class="flex justify-between items-center">
                        <span class="text-sm text-gray-600">Studenti Iscritti</span>
                        <span class="text-lg font-semibold text-gray-900">{{ $course->enrollments->count() }}</span>
                    </div>
                    <div class="flex justify-between items-center">
                        <span class="text-sm text-gray-600">Posti Disponibili</span>
                        <span class="text-lg font-semibold text-green-600">{{ $course->max_students - $course->enrollments->count() }}</span>
                    </div>
                    <div class="flex justify-between items-center">
                        <span class="text-sm text-gray-600">Occupazione</span>
                        <span class="text-lg font-semibold text-blue-600">
                            {{ $course->max_students > 0 ? round(($course->enrollments->count() / $course->max_students) * 100) : 0 }}%
                        </span>
                    </div>
                    @if($course->enrollments->count() > 0)
                    <div class="flex justify-between items-center">
                        <span class="text-sm text-gray-600">Ricavo Totale</span>
                        <span class="text-lg font-semibold text-green-600">
                            €{{ number_format($course->enrollments->count() * $course->price, 2) }}
                        </span>
                    </div>
                    @endif
                </div>

                <!-- Progress Bar -->
                <div class="mt-4">
                    <div class="flex justify-between text-sm text-gray-600 mb-1">
                        <span>Riempimento corso</span>
                        <span>{{ $course->enrollments->count() }}/{{ $course->max_students }}</span>
                    </div>
                    <div class="w-full bg-gray-200 rounded-full h-2">
                        <div class="bg-gradient-to-r from-rose-400 to-purple-500 h-2 rounded-full transition-all duration-300"
                             style="width: {{ $course->max_students > 0 ? ($course->enrollments->count() / $course->max_students) * 100 : 0 }}%"></div>
                    </div>
                </div>
            </div>

            <!-- Recent Enrollments -->
            @if($course->enrollments->count() > 0)
            <div class="bg-white rounded-lg shadow p-6">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">Studenti Iscritti</h3>
                <div class="space-y-3">
                    @foreach($course->enrollments->take(5) as $enrollment)
                    <div class="flex items-center space-x-3">
                        <div class="flex-shrink-0">
                            <div class="w-8 h-8 bg-gradient-to-r from-rose-400 to-purple-500 rounded-full flex items-center justify-center">
                                <span class="text-white font-medium text-xs">{{ strtoupper(substr($enrollment->user->name, 0, 1)) }}</span>
                            </div>
                        </div>
                        <div class="flex-1 min-w-0">
                            <p class="text-sm font-medium text-gray-900 truncate">{{ $enrollment->user->name }}</p>
                            <p class="text-xs text-gray-500">{{ $enrollment->enrollment_date?->format('d/m/Y') }}</p>
                        </div>
                    </div>
                    @endforeach

                    @if($course->enrollments->count() > 5)
                    <div class="text-center pt-2">
                        <a href="{{ route('admin.enrollments.index', ['course' => $course->id]) }}"
                           class="text-sm text-rose-600 hover:text-rose-800 font-medium">
                            Vedi tutti i {{ $course->enrollments->count() }} studenti →
                        </a>
                    </div>
                    @endif
                </div>
            </div>
            @endif
        </div>
    </div>
</div>
